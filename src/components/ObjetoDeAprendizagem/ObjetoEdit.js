import styled from 'styled-components'
import CardEdit from '../Card/CardEdit';

const ObjetoEdit = styled(CardEdit) `
    transform: translate(1742%, -15%);
    transition: 0.5s ease-in-out;
`

export default ObjetoEdit;