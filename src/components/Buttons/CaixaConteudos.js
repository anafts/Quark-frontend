import styled from 'styled-components';

const CaixaConteudos = styled.section `
    position: fixed;
    right: 5vw;
    top: 140px;

    
    .show {
        opacity: 1;
        visibility: visible;
    }
`

export default CaixaConteudos;